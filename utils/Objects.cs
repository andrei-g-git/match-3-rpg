public static class Objects{
    static void UpdatePropertyValuesInBulk(object source, object destination){
        //loop all the properties of the source and if the destination has them add/subtract/update them
        //if number add/subtract
        //if bool replace
        //etc
    }
}