using Constants;
using Godot.Collections;

public interface Collapsable{
    // public Array<TileNames> Transportables {get;}  //not all collapsables can transport, at least 4 tiles out of 12 can't 
    // public void AddTransportable(TileNames tile);
}